# frozen_string_literal: true
source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "cocoapods", "1.9.1"
gem "fastlane"
gem "xcpretty"

gem "rake" # workaround to fix installation of digest-crc (0.6.0)
